[build-system]
build-backend = "setuptools.build_meta"
requires = [ "setuptools>=64", "wheel" ]

[project]
name = "pyarmor_debug"
version = "0.0.1"
requires-python = ">=3.13,<3.14"

dependencies = [
    "pyarmor==9.2.3",
    "jupyter==1.1.1",
]
